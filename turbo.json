{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "//#check:tests": {},
    "//#check:types": {},
    "//#check:eslint": {},
    "//#check:prettier": {}
  }
}
