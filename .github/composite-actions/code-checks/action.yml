name: Code Checks
description: Checks code for compliance using linting, tests, etc.

runs:
  using: 'composite'
  steps:
    - name: Turbo Run All Checks
      run: pnpm run turbo:checks
      shell: bash
